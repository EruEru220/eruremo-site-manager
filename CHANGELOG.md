# CHANGELOG

このプロジェクトの変更履歴です。日付は日本時間（JST）です。

---

## Phase 3 ― 2026-08-06 ✅ 完了

ブランチ：`feature/r2-media-upload`（**GitHub へは push していません**）
未マージ・タグなし。`main` はまだ Phase 1 の状態です。

### 追加

- **埋め込み画像を、まとめて保管庫（R2）へ移す**（`eruremo_SiteManager.html`）
  メニューの「🗄 埋め込み画像を保管庫へ移す」で、これまで HTML の中に
  埋め込まれていた画像（Base64）を1枚ずつ保管庫へ送り、データを画像のURLに置き換えます。
  **同じ画像が何か所から使われていても、送るのは1回だけ**。置き換えは全部の場所に反映します。
  実行前に `.json` を自動で書き出し、`History.push()` も積むので **Ctrl+Z でも戻せます**。
  保存に失敗した画像は**埋め込みのまま残ります**（部分成功を許す。作業は止まりません）。
  **`toUrlMode()`（zip方式）はそのまま残しています**（オフライン運用の逃げ道）。
- **自動テスト 32件を追加**（合計 218件）
  - `test/r2_migrate.test.mjs` … 32件

### 修正

- **一括移行の前に、その場で保管庫の生死を確かめるようにした**
  手動確認で、`wrangler dev` を止めたあとに実行すると「つながりません」の案内が出ず、
  「0か所移した / 24か所残った」という結果になる不具合が見つかりました。
  `MediaAPI.probe()` がページを開いた時点の結果を覚えっぱなしにしていたためです。
  `mediaAlive()` を追加し、実行のたびに `/api/health` を確かめるようにしました。
  つながらなければ**バックアップ・履歴・確認ダイアログ・送信を一切しません**。
  ページを開いたあとに**復旧した**場合にちゃんと送れない問題も、あわせて直しています。
  詳しくは `非公開の旧 Phase 3 実施記録` §6-4。

### テスト結果まとめ

| 種別 | 件数 | 結果 |
|---|---|---|
| 自動テスト（モック・ネットワークなし） | 218 | ✅ 全成功 |
| 実地確認（`wrangler dev` へ実HTTP送信） | 48 | ✅ 全成功 |
| 手動確認（1回目） | — | 1件で不具合を発見（案内が出ない）→ 対処済み |
| 手動確認（対処後・全16項目） | 16 | ✅ 全成功 |

実データでの結果：**24か所（画像18枚）を移行／埋め込みのまま残った場所 0か所／
2.73MB → 14KB**、生成した `index.html` は **165KB**。
24か所に対して画像が18枚だったのは、同じ画像が6か所で共有されていたためです。

### 変更なし（意図的に触っていないもの）

- `legacy/eruremo_SiteManager_original.html` ／ `worker/` 一式
- `buildHtml()` / `migrate()` / `History` / `SCHEMA` / `TEMPLATE` の構造
- `toUrlMode()` / `shrinkImage()` / `handleFile()` / `storeImage()` / `MediaAPI.probe()`
- URL手入力欄（`.urlin`）、localStorage キー、`.json` の形式、生成 `index.html` の形式

### まだやっていないこと

- 画像一覧・削除（Phase 4）
- Cloudflare へのログイン・R2バケット作成・本番デプロイ・GitHub push
- **本番での移行のやり直し**
  今回入ったURLは `http://127.0.0.1:8787/media/…` です。Phase 3 は
  **仕組みを実装し、ローカル環境と実データで正常動作を確認する工程**であり、
  本番 R2・本番URLへの移行は **Phase 6/7 でやり直す別工程**です。
  **移行前のバックアップ `.json` は捨てないでください。**
  詳しくは `非公開の旧 Phase 3 実施記録` §7。

未検証項目の一覧は `非公開の旧 Phase 3 実施記録` §8 にあります（7項目）。

---

## Phase 2-2 ― 2026-08-05 ✅ 完了（手動確認 2026-08-06）

ブランチ：`feature/r2-media-upload`（**GitHub へは push していません**）
未マージ・タグなし。`main` はまだ Phase 1 の状態です。

### 追加

- **編集ツールから画像を保管庫（R2）へ保存**（`eruremo_SiteManager.html`）
  Worker と同じ住所から開いたときだけ、選んだ画像を保管庫へ送り、
  データには画像のURLを入れます。`file://` で開いたときは従来どおり Base64 です。
  対象は「画像をえらぶ」「ドラッグ＆ドロップ」「貼り付け」「ギャラリーのまとめて追加」。
  **失敗しても Base64 のまま作業を続行**します（つながらない・拒否・通信断すべて）。
- **画像の読み出しルート `GET /media/…`**（`worker/src/lib/mediaRead.js`）
  保存した画像をサムネイル・プレビューに表示するための読み取り専用の口。
  本番で `images.eruremo.com` が果たす役割を、ローカルで肩代わりします。
  キーは既存の `isValidMediaKey()` に完全一致したものだけを通し、
  Content-Type は保存時の申告ではなくキーの拡張子から決めます。
- **ローカル開発用の配信**（`npm run dev`）
  編集ツールを `worker/public/` へ複製してから `wrangler dev --local` を起動します。
  複製先は `.gitignore` 済みで、Git に入る本体は1つのままです。
- **画像の保存先を画面に常時表示**（手動確認で見つかった問題への対処）
  トーストは数秒で消えるため、利用者に届いていませんでした。
  トップバーに保存先の帯（🟢保管庫／⚪埋め込み／🟡つながりません）を、
  サムネイルには画像ごとの保存先（`🗄 保管庫` または容量）を出しっぱなしにします。
  あわせて、**保管庫につながらないときに黙って埋め込みへ切り替えない**よう修正しました。
  詳しくは `非公開の旧 Phase 2-2 実施記録` §6-4。
- **自動テスト 44件を追加**（合計 186件）
  - `test/media_upload.test.mjs` … 30件
  - `worker/test/mediaRead.test.mjs` … 14件

### テスト結果まとめ

| 種別 | 件数 | 結果 |
|---|---|---|
| 自動テスト（モック・ネットワークなし） | 186 | ✅ 全成功 |
| 実地確認（`wrangler dev` へ実HTTP送信） | 24 | ✅ 全成功 |
| 手動確認（利用者がブラウザで実施） | 6 | 5件成功／1件で問題を発見（通知が届かない）→ 対処済み |
| 手動確認（対処後の再確認・2026-08-06） | 8 | ✅ 全成功 |

### 変更なし（意図的に触っていないもの）

- `legacy/eruremo_SiteManager_original.html`
- `buildHtml()` / `migrate()` / `History` / `SCHEMA` / `TEMPLATE` の構造
- `shrinkImage()` / `toUrlMode()` / URL手入力欄（`.urlin`）
- localStorage キー、`.json` の形式、生成 `index.html` の形式
- 既存の Base64 画像（一括移行は Phase 3）

### まだやっていないこと

- 既存 Base64 画像の一括移行（Phase 3）／画像一覧・削除（Phase 4）
- Cloudflare へのログイン・R2バケット作成・本番デプロイ・GitHub push

未検証項目の一覧は `非公開の旧 Phase 2-2 実施記録` §7 にあります（7項目）。

---

## Phase 2-1 ― 2026-08-05 ✅ 完了

ブランチ：`feature/r2-media-upload`（**GitHub へは push していません**）
未マージ・タグなし。`main` はまだ Phase 1 の状態です。

### 修正（セキュリティ）

- **配布URLが平文で出力される不具合（C-1）を修正**
  あいことばの暗号化（`lockUrl()`）に失敗したとき、これまでは
  `console.error` だけで処理が続き、配布URLが暗号化されないまま
  生成 `index.html` に出力される可能性がありました。
  失敗した時点で書き出し自体を中止し、日本語のエラーを表示するようにしました。
  （`eruremo_SiteManager.html` / `README.md` の「セキュリティ上の注意」）

### 追加

- **Cloudflare Worker（メディアAPI）の土台**（`worker/`）
  - `GET /api/health` … 疎通確認
  - `POST /api/media/upload` … 画像アップロード（JPEG / PNG / WebP のみ）
  - 管理画面の静的ファイルと `/api/*` を、同じ Worker・同一オリジンで提供する構成
  - R2 のキーは Worker が生成し、クライアントからパスやファイル名を受け取らない
  - マジックバイトによる形式判定、SVG・HTML・JS 偽装ファイルの拒否
- **自動テスト 142件**（`node:test` のみ・モックのみ・ネットワークなし）
  - `test/lockurl_export.test.mjs` … 12件
  - `worker/test/**` … 130件
- **開発用依存 `wrangler` 4.118.0**（利用者の承認を得て導入）
  `wrangler dev --local` によるローカル実地確認 34項目に成功。
  本番に載る依存はゼロ（`npm audit --omit=dev` が 0件）。
- **文書**
  - `docs/PHASE2_1_IMPLEMENTATION_PLAN_JA.md` … 実装計画
  - `非公開の旧 Phase 2-1 実施記録` … 実施結果と未検証項目
  - `README.md` … 開発の始め方
  - `CHANGELOG.md` … この文書

### テスト結果まとめ

| 種別 | 件数 | 結果 |
|---|---|---|
| 自動テスト（モック・ネットワークなし） | 142 | ✅ 全成功 |
| 実地確認（`wrangler dev` へ実HTTP送信） | 34 | ✅ 全成功 |
| 手動確認（利用者がブラウザで実施） | 7 | ✅ 全成功 |

手動確認では、あいことば付きの配布物を作って `index.html` を保存し、
**生成ファイルに配布URLが平文で含まれていないこと**を実際に確認しました。

### 変更なし（意図的に触っていないもの）

- `legacy/eruremo_SiteManager_original.html`（SHA-256 `42FD31BB…4BF9DF51`）
- 画像フィールドUI・Base64 画像処理・URL手入力欄
- localStorage キー（`elremo_editor_data_v9` / `elremo_editor_data_v1`）
- `.json` の形式、`buildHtml()` / `migrate()` / `History` / `SCHEMA` / `TEMPLATE` の構造
- 生成 `index.html` の形式、`lock` の暗号方式

### まだやっていないこと（Phase 2-2 以降）

- 管理画面から R2 へのアップロード接続（**Phase 2-2**）
- `docs/TEST_PLAN_JA.md` の全項目（実施したのは基本動作の7項目）
- Cloudflare へのログイン・R2バケット作成・DNS変更・本番デプロイ
- GitHub の remote 登録・push

未検証項目の一覧は `非公開の旧 Phase 2-1 実施記録` §6 にあります（8項目）。

---

## [phase-1] Phase 1 ― 解析・バックアップ・Git準備・設計

- 原本を `legacy/` に保存
- 解析・リスク分析・Phase 計画・テスト計画・Phase 2 仕様書を作成（`docs/` 7本）
- 開発ルール `旧内部開発ルール` を作成
